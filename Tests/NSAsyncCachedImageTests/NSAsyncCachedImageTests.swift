import XCTest
@testable import NSAsyncCachedImage

final class NSAsyncCachedImageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(NSAsyncCachedImage().text, "Hello, World!")
    }
}
